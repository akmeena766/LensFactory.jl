# LensModel

The `LensModel` module is the interface between the user and 

```@docs
LensModel.read_input
LensModel.fit_model
LensModel.save_best_fit
LensModel.get_potential
LensModel.get_deflection
LensModel.get_jacobian
```